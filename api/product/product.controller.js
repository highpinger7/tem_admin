const { addProduct } = require("./product.service");

module.exports = {
    addNewProduct:(req,res) => {
        const body =req.body;
        // addProduct(body,(error,results))
    }
}

function getXlsFileList() {}